#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../xlsjs/bin/xls.njs" "$@"
  ret=$?
else 
  node  "$basedir/../xlsjs/bin/xls.njs" "$@"
  ret=$?
fi
exit $ret
